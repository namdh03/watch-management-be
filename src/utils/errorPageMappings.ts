const errorPageMappings: { [key: string]: string } = {
  '/admin/brand/create': 'create-brand',
  '/admin/brand/update': 'update-brand'
  // Add more routes here
  // '/another-route': 'another-view',
}

export default errorPageMappings
